{
  "hooks": {},
  "note": "techtrip-secondbrain is an installer/orchestrator; the vault runtime hooks (SessionStart hot-cache, PostToolUse git auto-commit, Stop, PostCompact) are provided by the claude-obsidian plugin this bootstraps. No hooks are defined here for the MVP. The empty `hooks` object is the schema-valid 'no hooks' form and a future extension point."
}
